import type * as Cohere from "../index";
/**
 * A single parsed page. The payload shape depends on `output_format`.
 */
export type ParsePage = Cohere.ParsePage.Blocks | Cohere.ParsePage.Markdown;
export declare namespace ParsePage {
    interface Blocks extends Cohere.ParseBlocksPageVariant {
        type: "blocks";
    }
    interface Markdown extends Cohere.ParseMarkdownPageVariant {
        type: "markdown";
    }
}
